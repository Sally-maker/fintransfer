spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/fintransfer}
    username: ${SPRING_DATASOURCE_USERNAME:fintransfer}
    password: ${SPRING_DATASOURCE_PASSWORD:fintransfer}

  jpa:
    show-sql: false

  flyway:
    clean-disabled: false
    baseline-on-migrate: false

anthropic:
  api-key: test-key
  model: claude-haiku-4-5-20251001
  base-url: https://api.anthropic.com
